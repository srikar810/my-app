import { HighlightDirDirective } from './highlight-dir.directive';

describe('HighlightDirDirective', () => {
  it('should create an instance', () => {
    const directive = new HighlightDirDirective();
    expect(directive).toBeTruthy();
  });
});
